import 'package:flutter/material.dart';

class ActiveEdgeApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: "Active Edge",

    );
  }
}
